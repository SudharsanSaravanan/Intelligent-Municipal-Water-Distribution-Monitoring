# ── Server ─────────────────────────────────────────────────────
PORT=3000

# ── Firebase ────────────────────────────────────────────────────
FIREBASE_DATABASE_URL=https://hydronet-monitor-default-rtdb.firebaseio.com
FIREBASE_KEY_PATH=./serviceAccountKey.json

# ── CORS ────────────────────────────────────────────────────────
# Comma-separated list of allowed frontend origins
CORS_ORIGIN=http://localhost:5500

# ── ML Pipeline (Python microservice) ──────────────────────────
ML_SERVICE_URL=http://localhost:5050
ML_SERVICE_PORT=5050
ML_LOG_LEVEL=INFO

# ── MQTT Broker (for valve control via LoRa gateway) ───────────
MQTT_BROKER_HOST=localhost
MQTT_BROKER_PORT=1883
